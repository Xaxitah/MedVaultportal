Circular gradient chip that marks an exam (P1/P2) or a topic number. The signature MedVault accent.

```jsx
<ExamBadge exam="p1">P1</ExamBadge>
<ExamBadge exam="p2" size={32}>T07</ExamBadge>
```

`exam` selects blue (p1) or purple (p2) gradient. `size` controls diameter.
