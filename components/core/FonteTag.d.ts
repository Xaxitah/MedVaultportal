import * as React from "react";

export interface FonteTagProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}

/** Bordered source-reference chip for crediting textbooks / slides. */
export function FonteTag(props: FonteTagProps): React.ReactElement;
