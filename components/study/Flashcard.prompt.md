3D flip card. Front carries the prompt, navy back the answer. Click to flip (or control via `flipped`/`onFlip`).

```jsx
<Flashcard
  front="Qual o mecanismo de ação dos AINEs?"
  back="Inibição da enzima ciclo-oxigenase (COX-1 e COX-2)."
/>
```
